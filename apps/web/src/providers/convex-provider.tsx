"use client";

import type { ReactNode } from "react";

/** No-op stub — Convex is not used in CENTAUR local mode. */
export const ConvexClientProvider = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};
